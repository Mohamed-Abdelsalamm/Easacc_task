import 'package:flutter/material.dart';

class SetLinkView extends StatelessWidget {
  const SetLinkView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('Set Link View'));
  }
}
